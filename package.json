{
  "name": "wos-ally-manager",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,svelte,js,json,md,css}\" --ignore-path .gitignore"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "prettier": "^3.3.3",
    "prettier-plugin-svelte": "^3.2.6",
    "eslint": "^9.15.0",
    "typescript": "^5.7.3"
  },
  "overrides": {
    "serialize-javascript": "7.0.3",
    "cookie": "^0.7.0",
    "esbuild": "^0.25.0"
  }
}
